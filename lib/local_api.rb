class LocalApi
  def sync_vendor(vendor)
    # upserts the vendor and returns its id
    'vendor001'
  end

  def sync_project(project)
    # upserts the project and returns its id
    'project001'
  end

  def add_vendor_to_project(vendor, project)
    # adds vendor to project; possibly a noop
  end
end
